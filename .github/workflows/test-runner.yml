name: Test self-hosted runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted]

    steps:
      - name: Who am I
        run: |
          whoami
          hostname
          docker --version
          docker run --rm hello-world
